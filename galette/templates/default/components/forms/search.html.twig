{% include "components/forms/input.html.twig" with {
        type: "text",
        name: entry.field_id,
        id: entry.field_id,
        required: entry.required,
        disabled: entry.disabled,
        label: entry.label,
        autocomplete: "off",
        value: null,
        component_id: entry.field_id ~ '_field',
        component_class: 'ui search field',
        elt_class: 'prompt'
    }
%}
