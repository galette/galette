{#
 # This file is part of Galette (https://galette.eu).
 # SPDX-FileCopyrightText: Copyright © 2003-2026 The Galette Team
 # SPDX-License-Identifier: GPL-3.0-or-later
 #}

{% extends "components/forms/select.html.twig" %}

{% block element %}
    <div id="{{ id }}" class="jsonly search-dropdown ui input paginated">
        <input id="{{ id }}_input" type="hidden" name="{{ id }}" value="{% if value is not same as null %}{{ value }}{% endif %}" placeholder="{{ _T("Member ID") }}">
        <i class="jsonly displaynone dropdown icon"></i>
        {# Both buttons are kept out of the keyboard path on purpose: Fomantic
         # closes the menu as soon as its search input loses the focus, so a Tab
         # towards the pagination closes the very list it pages through. Drop
         # both tabindex once the dropdown keeps its menu open across the
         # controls sitting beside it. #}
        <button type="button" class="ui mini compact icon disabled button prev-results" aria-label="{{ _T("Load previous members...") }}" tabindex="-1">
            <i class="jsonly displaynone chevron circle left icon disabled button tooltip" title="{{ _T("Load previous members...") }}" aria-hidden="true"></i>
        </button>
        <button type="button" class="ui mini compact icon disabled button next-results" aria-label="{{ _T("Load following members...") }}" tabindex="-1">
            <i class="jsonly displaynone chevron circle right icon disabled button tooltip" title="{{ _T("Load following members...") }}" aria-hidden="true"></i>
        </button>
        <div class="jsonly displaynone default text">{% if placeholder is defined %}{{ placeholder }}{% else %}{{ _T("Search for name or ID and pick member") }}{% endif %}</div>
        <div class="jsonly displaynone menu">
    {% for k, v in members.list %}
            <div class="item" data-value="{{ k }}">{{ v }}</div>
    {% endfor %}
        </div>
    </div>
{% endblock %}